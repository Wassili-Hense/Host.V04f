<UserControl x:Class="X13.UI.ValueControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Grid VerticalAlignment="Top" Margin="10, 0, 0, 0">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="auto" MinWidth="80"/>
      <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition MinHeight="20"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Border Grid.Row="0" Grid.ColumnSpan="2" BorderThickness="0,1" Background="White" BorderBrush="#FFD4D4D4" />
    <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding name}" Margin="0,2" />
    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding valueStr}"  Margin="0,2" />
    <ItemsControl Grid.Row="1" Grid.ColumnSpan="2" ItemsSource="{Binding fields}">
    </ItemsControl>
  </Grid>
</UserControl>
