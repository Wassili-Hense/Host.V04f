<x13ui:UiBaseForm x:Class="X13.UI.InspectorForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:x13ui="clr-namespace:X13.UI"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="600">
    <Grid Background="White">
    <Grid.Resources>
      <DataTemplate x:Key="ChildrenDataTemplate">
        <Border BorderBrush="Black" BorderThickness="1" Margin="5" VerticalAlignment="Top">
        <StackPanel MouseUp="StackPanel_MouseUp">
          <Label HorizontalAlignment="Center" Content="{Binding Path=name}" Margin="5" />
        </StackPanel>
        </Border>
      </DataTemplate>
    </Grid.Resources>
      <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition/>
    </Grid.RowDefinitions>
    <ContentControl Content="{Binding valueVC}" Grid.IsSharedSizeScope="True" />
    <ItemsControl ItemTemplate="{StaticResource ChildrenDataTemplate}" ItemsSource="{Binding Path=data.children}" Grid.Row="1" >
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <StackPanel Orientation="Horizontal"/>
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
    </ItemsControl>
  </Grid>
</x13ui:UiBaseForm>
